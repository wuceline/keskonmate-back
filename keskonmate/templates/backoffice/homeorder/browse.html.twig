{% extends 'backoffice/back/main.html.twig' %}

{% block title %} - 5 series{{ parent() }}{% endblock %}


{% block body %}
<div class="container">
{{ include('_partials/_flash_messages.html.twig') }}

<h1>Series en page d'accueil</h1>

<div class="text-end">
</a>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">Emplacement</th>
      <th scope="col">Titre</th>
    </tr>
  </thead>
  <tbody>
{% for series_homeOrder in series_homeOrder %}
    <tr>
      <th scope="row">{{ series_homeOrder.homeOrder }}</th>
      <td>{{ series_homeOrder.title }}</td>
    </tr>
{% endfor %}
  </tbody>
</table>
</div>




<div class="row">
        <div class="col-12">
            <form action="" method="POST" class="mt-5">
                <div class="row">
                    <div class="col">
                        <div class="form-group">
                            <label for="emplacement1">Emplacement #1</label>
                            <select class="form-control" id="emplacement1" name="emplacement[]">
                                  <option value="">choisissez :</option>
                                {% for series in series_list %}
                                  <option value="{{ series.id }}">{{ series.title }}
                                  </option>
                                {% endfor %}
                            </select>
                        </div>
                    </div>
                    <div class="col">
                        <div class="form-group">
                            <label for="emplacement1">Emplacement #2</label>
                            <select class="form-control" id="emplacement1" name="emplacement[]">
                                  <option value="">choisissez :</option>
                                {% for series in series_list %}
                                  <option value="{{ series.id }}">{{ series.title }}
                                  </option>
                                {% endfor %}
                            </select>
                        </div>
                    </div>
                    <div class="col">
                        <div class="form-group">
                            <label for="emplacement1">Emplacement #3</label>
                            <select class="form-control" id="emplacement1" name="emplacement[]">
                                  <option value="">choisissez :</option>
                                {% for series in series_list %}
                                  <option value="{{ series.id }}">{{ series.title }}
                                  </option>
                                {% endfor %}
                            </select>
                        </div>
                    </div>
                    <div class="col">
                        <div class="form-group">
                            <label for="emplacement1">Emplacement #4</label>
                            <select class="form-control" id="emplacement1" name="emplacement[]">
                                  <option value="">choisissez :</option>
                                {% for series in series_list %}
                                  <option value="{{ series.id }}">{{ series.title }}
                                  </option>
                                {% endfor %}
                            </select>
                        </div>
                    </div>
                    <div class="col">
                        <div class="form-group">
                            <label for="emplacement1">Emplacement #5</label>
                            <select class="form-control" id="emplacement1" name="emplacement[]">
                                  <option value="">choisissez :</option>
                                {% for series in series_list %}
                                  <option value="{{ series.id }}">{{ series.title }}
                                  </option>
                                {% endfor %}
                            </select>
                        </div>
                    </div>
                    
                <button type="submit" class="btn btn-primary btn-block mt-5">Valider</button>
                {# <input type="hidden" name="token" value="<?= $_SESSION['token'] ?>"> #}
            </form>
        </div>
    </div>
{% endblock %}