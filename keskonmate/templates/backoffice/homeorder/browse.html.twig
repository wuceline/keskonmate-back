{% extends 'backoffice/back/main.html.twig' %}

{% block title %} - 5 series{{ parent() }}{% endblock %}


{% block body %}
<div class="container">
{{ include('_partials/_flash_messages.html.twig') }}

<h1>Series en page d'accueil</h1>

<div class="text-end">
</a>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">Emplacement</th>
      <th scope="col">Titre</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
{% for series_homeOrder in series_list %}
{% if series_homeOrder.homeOrder > 0 and series_homeOrder.homeOrder < 6 %}
    <tr>    
      <th scope="row">{{ series_homeOrder.homeOrder }}</th>
      <td>{{ series_homeOrder.title }}</td>
      <td>
        <button type="submit" class="btn btn-primary">
          Mettre a jour
        </button>
      </td>
    </tr>
{% endif %}
{% endfor %}
  </tbody>
</table>
</div>
{% endblock %}